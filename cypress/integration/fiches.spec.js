const faker = require('faker')

context('Fiches', () => {
    it('should show validation error if missing fields', () => {

    })

    it('should submit a fiche if all details filled', () => {

    })
})
