models:

  Document:
    movie_id: integer
    document_type: string
    filename: string
    comments: string
    relationships:
      belongsTo: Movie

controllers:

  Document:
    index:
      query: all
      render: documents.index with Documents

seeders: Document
