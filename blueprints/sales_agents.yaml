models:

  SalesAgent:
    movie_id: integer
    name: string
    country_id: integer
    contact_person: string
    email: string
    relationships:
      belongsTo: Country, Movie

controllers:

  SalesAgent:
    index:
      query: all
      render: sales_agent.index with salesAgents


seeders: SalesAgent