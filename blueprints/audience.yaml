models:

  Audience:
    name: string
    type: string
    relationships:
      belongsTo: Movie

controllers:
  Audience:
    index:
      query: all
      render: audience.index with:audiences

seeders: Audience
