models:

  Document:
    media_id: integer
    document_type: string
    filename: string
    comments: string
    relationships:
      belongsTo: Media

controllers:

  Document:
    index:
      query: all
      render: documents.index with Documents

seeders: Document